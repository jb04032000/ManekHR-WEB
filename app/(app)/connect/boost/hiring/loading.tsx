// Loading skeleton for the boost composer route. All boost composer routes render
// the same <BoostComposer>, so they share one skeleton
// (features/connect/ads/BoostComposerSkeleton).
export { default } from '@/features/connect/ads/BoostComposerSkeleton';
