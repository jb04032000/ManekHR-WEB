// Loading skeleton for /connect/boost/listing/[listingId]. The listing / job /
// post boost routes all render the same <BoostComposer>, so they share one
// skeleton (features/connect/ads/BoostComposerSkeleton).
export { default } from '@/features/connect/ads/BoostComposerSkeleton';
