import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Employee Code Settings',
};

export default function EmployeeCodeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
