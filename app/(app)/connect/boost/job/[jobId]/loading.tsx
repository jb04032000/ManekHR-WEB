// Loading skeleton for /connect/boost/job/[jobId]. The listing / job / post
// boost routes all render the same <BoostComposer>, so they share one skeleton
// (features/connect/ads/BoostComposerSkeleton).
export { default } from '@/features/connect/ads/BoostComposerSkeleton';
