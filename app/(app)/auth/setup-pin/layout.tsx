import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Set your App PIN',
};

export default function SetupPinLayout({ children }: { children: React.ReactNode }) {
  return children;
}
