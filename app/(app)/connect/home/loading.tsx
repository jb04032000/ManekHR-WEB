import ProfileSkeleton from '@/features/connect/profile/ProfileSkeleton';

/** Route-level loading UI while the Connect smart-entry server component resolves. */
export default function Loading() {
  return <ProfileSkeleton />;
}
